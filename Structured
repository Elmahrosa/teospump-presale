/frontend  → Next.js code
/backend   → Node.js watcher
presaleConfig.json → Editable pricing & address config
README.md → Setup + deployment instructions
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/Structured/index.html"
    }
  ]
}
